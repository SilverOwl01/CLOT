// src/config/constants.js

// Identificador de la aplicación en la ruta de Firestore [cite: 7]
export const APP_ID = 'clot-app-produccion';
export const BASE_PATH = `artifacts/${APP_ID}/public/data`;